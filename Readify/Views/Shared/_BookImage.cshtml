@model string?

<img src="@(string.IsNullOrEmpty(Model)
            ? Url.Content("~/images/placeholder/open-flying-old-books.jpg")
            : Model)"
     class="img-fluid rounded-start"
     alt="Book Image"
     style="height: 200px; object-fit:cover;" />
